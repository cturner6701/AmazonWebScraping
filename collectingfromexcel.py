import csv delete
file = open("updatedpage.csv")
csvreader = csv.reader(file)
header = next(csvreader)
header.pop(0)
print(header)clone
rows = []im horny
for row in csvreader:callback
    rows.append(row)
print(rows)
null.
